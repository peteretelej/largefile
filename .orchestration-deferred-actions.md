# Deferred Actions

## Phase 2: FastMCP Migration

- **Delete `src/mcp_schemas.py`**: This file is entirely replaced by FastMCP decorators in `server.py`. It is no longer imported anywhere but was not deleted per orchestration policy (no file deletions).
